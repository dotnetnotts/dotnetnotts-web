@page "/speakerinfo"
@{
    const int tabindex = 0;
}

<article data-bind="page: { id: 'speakerinfo', title: 'Speaker Information - dot net notts', sourceOnShow: 'speakerinfo.html', withOnShow: globalOnShow, sourceCache: true }" style="display: block;">

    <header>
        <div class="header-text">
            <h1 tabindex="@(tabindex)">Speaker Information</h1>
        </div>
        <hr/>
    </header>

    <section id="all-events" aria-labelledby="all-events-heading">
        <div class="row">
            <div class="col-xs-12">
                <h2 id="all-events-heading" tabindex="@(tabindex)">For All Events</h2>
                <h3 tabindex="@(tabindex)">Code Of Conduct</h3>
                <p tabindex="@(tabindex)">
                    Please familiarise yourself with the <a id="code-of-conduct-link" tabindex="@(tabindex)" href="~/codeofconduct" target="_blank" aria-label="Code of Conduct (opens in new tab)">Code of Conduct</a>
                    before speaking at, or attending the event.
                </p>
                <p tabindex="@(tabindex)">
                    Should you have any concerns regarding the Code of Conduct before, during or after the event, please contact the organisers.
                </p>               
            </div>
        </div>
    </section>

    <section id="virtual-events" aria-labelledby="virtual-events-heading">
        <div class="row">
            <div class="col-xs-12">
                <h2 id="virtual-events-heading" tabindex="@(tabindex)">Virtual Meetups</h2>

                <p tabindex="@(tabindex)">
                    While social-distancing is still in place, the .NET Notts meetup events have moved to being virtual. This is great because not only can we still gather in a
                    safer environment, but the reach for your talks goes a lot further. We have people not attending the events nationwide.
                </p>
                <p tabindex="@(tabindex)">
                    This section has details for anything you may need to know before presenting at one of the virtual events
                </p>

                <!-- Tech Section -->
                <h3 tabindex="@(tabindex)" id="tech">Tech</h3>
                <p tabindex="@(tabindex)">

                </p>

                <h4 tabindex="@(tabindex)" id="zoom">Zoom</h4>
                <p tabindex="@(tabindex)">
                    The meetups are hosted on Zoom. We suggest you have an install before the event.
                </p>

                <h4 tabindex="@(tabindex)" id="youtube">YouTube Streaming</h4>
                <p tabindex="@(tabindex)">
                    All the talks are streamed to YouTube so that they are accessible to a larger audience.
                </p>

                <h4 tabindex="@(tabindex)" id="hardware">Hardware</h4>
                <p tabindex="@(tabindex)">
                    All the talks are streamed to YouTube so that they are accessible to a larger audience.
                </p>
                <ul>
                    <li>Webcam</li>
                    <li>Microphone</li>
                </ul>
                <br>
            </div>
        </div>
    </section>

    <section id="schedule">
        <div class="row">
            <div class="col-xs-12">
                <h2 tabindex="@(tabindex)">Rough Schedule</h2>
                <p tabindex="@(tabindex)">
                    Before the event Pete, Connell and/or Jess will be in touch to make sure you have all the information you may need. All the times listed are in BST.
                </p>

                <h3 tabindex="@(tabindex)" id="event">The Event</h3>
            <h4 tabindex="@(tabindex)">18.00: Tech check and chatter. </h4>
            <p tabindex="@(tabindex)">
                This is the chance to do any last minute checks for tech and sharing with an organiser. At this point speakers can decide between themselves
                the order in which they will present. None of the attendees will be present at this point.
            </p>
            <h4 tabindex="@(tabindex)">18.30: Event Opened. </h4>
            <p tabindex="@(tabindex)">
                Attendees will be allowed into the room and there will be some general chit-chat. The event will be opened with some slides from the organisers.
            </p>
            <h4 tabindex="@(tabindex)">19.00: First Talk (if there are two). </h4>
            <p tabindex="@(tabindex)">
                We can adapt to other talk lengths if needed.
            </p>
            <h4 tabindex="@(tabindex)">20.00: Break. </h4>
            <p tabindex="@(tabindex)">
                Quick 10 minute break to grab a drink and stretch your legs.
            </p>
            <h4 tabindex="@(tabindex)">20.10: Second Talk (if there are two). </h4>
            <p tabindex="@(tabindex)">
                We can adapt to other talk lengths if needed.
            </p>
            <h4 tabindex="@(tabindex)">21.10: Meetup Finished </h4>
            <p tabindex="@(tabindex)">
                The Zoom call is left open for anyone who wants to keep chatting.
            </p>
            <br>
        </div>
    </div>
</section>

    <section id="social-media">
        <div class="row">
            <div class="col-xs-12">
                <h2 tabindex="@(tabindex)">Social Media</h2>
                <ul>
                    <li tabindex="@(tabindex)">
                        If you want to take photos containing the attendees images, please let us know beforehand. We will arrange a point where we request that anyone who does not want to be included in a photo turn off their cameras.<br>
                    </li>
                    <li tabindex="@(tabindex)">
                        Feel free to share the recordings of your talks.
                    </li>
                </ul>
            </div>
        </div>
    </section>
    <br/>
    <br/>
</article>
